# Variables pour les commandes Prisma manuelles
# Usage: npx dotenv -e .env.test -- npx prisma migrate deploy
# Note: Vitest utilise sa propre config (vitest.config.js > test.env)

DATABASE_URL="file:./test.db"
JWT_SECRET="test-secret-key-minimum-32-characters-long"
JWT_EXPIRES_IN="1h"
NODE_ENV="test"
PORT="3001"
